{
  "name": "mookr-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "mookf — escape Notion / Lark, sync into a local OKF Markdown knowledge base. CLI for humans & Claude Code.",
  "scripts": {
    "build": "pnpm --filter @mookf/cli build",
    "typecheck": "pnpm --filter @mookf/cli typecheck",
    "start": "pnpm --filter @mookf/cli start",
    "clean": "pnpm --filter @mookf/cli clean"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0"
}
